# keiba

JRA重賞の予想・検証リポジトリ。記憶や一般知識で手順・数値を代用しない。

## 正本の所在
- 工程・ゲート：`プロジェクト指示_v2.md`
- 失敗則：`log/rules_master.csv`（全行。番号での参照はしない）
- 集計値・n件数・ROI：`log/` 配下CSV（`analyze.py` の出力が正）
- 暫定ルール・較正カウンタの現在値：`log/README.md`
- 評価体系・コース・レース傾向：`references/` 配下（1データ1ファイル）
- 公開ボード：`docs/` 配下（派生成果物。正本は `log/*.csv`）

## `reflection/` の扱い（書き出し専用）
`reflection/` は**人間が読むためのアウトプット**であり、工程の入力ではない。
- **工程の根拠として参照しない**（予想・集計・ルール判定・原因分析の入力は `log/` 配下CSVと一次ソースのみ）
- 読みに行くのは、ユーザーが**回顧そのもの**を求めたときだけ（内容の照会・改訂・書き出し）
- 振り返りV6.5の書き出しは従来どおり必須。CSVと食い違ったらCSVが正

## セッション冒頭
`git pull` → `KEIBA_LOCAL=1 bash tools/claude_run.sh` で validate を通す。
ERROR があれば本題より先に不整合と修正行を報告する。

## 書き込み
ログ・ナレッジは直接編集してよいが、編集内容は必ずチャットで要約提示する。
予想ボード公開時は `log/*.csv` の確定後に `tools/publish_board.py` を使い、CSVと `docs/` を同一コミット候補として提示する。
コミットはユーザーが行う。`cache/` は生成物なので触らない。

## 禁止
- 取得に失敗したデータを推測で埋める（「取得失敗」と明示する）
- 結果・配当をチャット記憶から転記する（一次ソース確認後のみ）